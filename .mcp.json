{
  "mcpServers": {
    "slotmachine": {
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}", "python", "-m", "slotmachine.server"],
      "env": {
        "SLOTMACHINE_CONFIG": "${USERPROFILE}/.slotmachine/settings.env"
      },
      "description": "SlotMachine MCP server — Obsidian vault GraphDB sync & RAG"
    }
  }
}
